[package]
name = "schema-tui"
version = "0.1.0"
edition = "2021"
description = "Interactive accordion TUI for building JSON request bodies from a JSON Schema."

[dependencies]
anyhow = "1.0"
crossterm = "0.29"
ratatui = "0.30"
schema-doc = { path = "../schema-doc" }
schemars = "0.8.22"
serde_json = "1.0"
